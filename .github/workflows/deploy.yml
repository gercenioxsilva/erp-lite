name: Deploy to AWS — prod

on:
  push:
    branches: [main]

# AWS_REGION matches terraform/variables.tf default (us-east-1).
# All other runtime values come from Terraform outputs — no extra secrets needed.
env:
  AWS_REGION:        us-east-1
  API_ECR_REPO:      erp-lite/api-core
  TF_STATE_BUCKET:   erp-lite-terraform-state

jobs:
  deploy:
    name: Build → ECR → Migrate → Terraform → S3
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      # ── AWS credentials ───────────────────────────────────────────────────────
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}

      # ── Bootstrap: S3 state bucket ────────────────────────────────────────────
      # Terraform needs this bucket to exist BEFORE terraform init.
      # On every run we check and create if missing — idempotent.
      - name: Ensure Terraform state bucket exists
        run: |
          if aws s3api head-bucket --bucket "$TF_STATE_BUCKET" 2>/dev/null; then
            echo "State bucket already exists."
          else
            echo "Creating state bucket $TF_STATE_BUCKET..."
            aws s3api create-bucket \
              --bucket "$TF_STATE_BUCKET" \
              --region "$AWS_REGION"
            aws s3api put-bucket-versioning \
              --bucket "$TF_STATE_BUCKET" \
              --versioning-configuration Status=Enabled
            aws s3api put-bucket-encryption \
              --bucket "$TF_STATE_BUCKET" \
              --server-side-encryption-configuration \
                '{"Rules":[{"ApplyServerSideEncryptionByDefault":{"SSEAlgorithm":"AES256"}}]}'
            echo "State bucket created."
          fi

      # ── Terraform init ────────────────────────────────────────────────────────
      # terraform_wrapper: false so "terraform output" returns raw values
      # without the wrapper's extra stderr that breaks variable capture.
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.9"
          terraform_wrapper: false

      - name: Terraform Init
        working-directory: terraform
        run: terraform init

      # ── Bootstrap: ensure ECR repo exists before Docker push ─────────────────
      # On first deploy the ECR repository doesn't exist yet.
      # We target only the ECR resources so the ECS service is not touched.
      # On subsequent runs this is a fast no-op (resources already in state).
      - name: Terraform Apply — bootstrap ECR
        working-directory: terraform
        env:
          TF_VAR_db_password: ${{ secrets.TF_VAR_DB_PASSWORD }}
          TF_VAR_jwt_secret:  ${{ secrets.TF_VAR_JWT_SECRET }}
        run: |
          terraform apply -auto-approve \
            -target=aws_ecr_repository.api_core \
            -target=aws_ecr_lifecycle_policy.api_core

      # ── Read current-state values for migration step (pre-deploy) ────────────
      # Reads subnet/SG/cluster needed to run the migration ECS task.
      # Returns empty strings on first deploy (handled by the if condition below).
      - name: Read pre-deploy Terraform outputs
        id: tf-pre
        working-directory: terraform
        run: |
          echo "cluster=$(terraform output -raw ecs_cluster_name 2>/dev/null || true)"              >> $GITHUB_OUTPUT
          echo "task_family=$(terraform output -raw ecs_task_definition_family 2>/dev/null || true)" >> $GITHUB_OUTPUT
          echo "subnets=$(terraform output -raw public_subnet_ids 2>/dev/null || true)"             >> $GITHUB_OUTPUT
          echo "sg=$(terraform output -raw api_security_group_id 2>/dev/null || true)"             >> $GITHUB_OUTPUT

      # ── Compute ECR registry URL from current AWS account ─────────────────────
      # {account_id}.dkr.ecr.{region}.amazonaws.com — no secret needed.
      - name: Compute ECR registry URL
        id: ecr
        run: |
          ACCOUNT=$(aws sts get-caller-identity --query 'Account' --output text)
          echo "registry=${ACCOUNT}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com" >> $GITHUB_OUTPUT

      # ── Build & push api-core Docker image ───────────────────────────────────
      - name: Log in to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push api-core image
        uses: docker/build-push-action@v5
        with:
          context: services/api-core
          target: production
          push: true
          tags: |
            ${{ steps.ecr.outputs.registry }}/${{ env.API_ECR_REPO }}:${{ github.sha }}
            ${{ steps.ecr.outputs.registry }}/${{ env.API_ECR_REPO }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ── Run DB migrations BEFORE new service version goes live ───────────────
      # Uses the existing task definition (IAM roles + env vars) but overrides
      # the container image with the new SHA so new migration files run.
      # Skipped on first deploy when there is no ECS cluster in state yet.
      - name: Run migrations via ECS run-task
        if: ${{ steps.tf-pre.outputs.cluster != '' }}
        run: |
          IMAGE="${{ steps.ecr.outputs.registry }}/${{ env.API_ECR_REPO }}:${{ github.sha }}"
          OVERRIDES=$(jq -n --arg img "$IMAGE" \
            '{"containerOverrides":[{"name":"api-core","image":$img,"command":["npm","run","migrate"]}]}')

          TASK_ARN=$(aws ecs run-task \
            --cluster "${{ steps.tf-pre.outputs.cluster }}" \
            --task-definition "${{ steps.tf-pre.outputs.task_family }}" \
            --launch-type FARGATE \
            --network-configuration "awsvpcConfiguration={subnets=[${{ steps.tf-pre.outputs.subnets }}],securityGroups=[${{ steps.tf-pre.outputs.sg }}],assignPublicIp=ENABLED}" \
            --overrides "$OVERRIDES" \
            --count 1 \
            --query 'tasks[0].taskArn' \
            --output text)

          echo "Migration task ARN: $TASK_ARN"

          aws ecs wait tasks-stopped \
            --cluster "${{ steps.tf-pre.outputs.cluster }}" \
            --tasks "$TASK_ARN"

          EXIT=$(aws ecs describe-tasks \
            --cluster "${{ steps.tf-pre.outputs.cluster }}" \
            --tasks "$TASK_ARN" \
            --query 'tasks[0].containers[0].exitCode' \
            --output text)

          if [ "$EXIT" != "0" ]; then
            echo "::error::Migration failed with exit code $EXIT"
            exit 1
          fi
          echo "Migrations completed successfully."

      # ── Terraform Apply: full infra + ECS rolling deploy ─────────────────────
      # TF_VAR_image_tag sets var.image_tag → new task definition revision →
      # aws_ecs_service updates to the new revision → rolling deploy starts.
      # Also creates VPC, RDS, ALB, S3 bucket and CloudFront on first run.
      - name: Terraform Apply
        working-directory: terraform
        env:
          TF_VAR_db_password: ${{ secrets.TF_VAR_DB_PASSWORD }}
          TF_VAR_jwt_secret:  ${{ secrets.TF_VAR_JWT_SECRET }}
          TF_VAR_image_tag:   ${{ github.sha }}
        run: terraform apply -auto-approve

      # ── Read post-deploy outputs ──────────────────────────────────────────────
      # S3 bucket + CloudFront are guaranteed to exist after the apply above.
      - name: Read post-deploy Terraform outputs
        id: tf-post
        working-directory: terraform
        run: |
          echo "s3_bucket=$(terraform output -raw s3_bucket_name)"           >> $GITHUB_OUTPUT
          echo "cf_id=$(terraform output -raw cloudfront_distribution_id)"   >> $GITHUB_OUTPUT
          echo "api_url=$(terraform output -raw api_url)"                    >> $GITHUB_OUTPUT
          echo "cf_domain=$(terraform output -raw cloudfront_domain)"        >> $GITHUB_OUTPUT

      # ── Build backoffice ──────────────────────────────────────────────────────
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build backoffice
        run: npm run build --workspace=apps/backoffice
        env:
          VITE_API_URL: ${{ steps.tf-post.outputs.api_url }}

      # ── Deploy backoffice to S3 + CloudFront ─────────────────────────────────
      - name: Sync backoffice to S3
        run: |
          aws s3 sync apps/backoffice/dist \
            "s3://${{ steps.tf-post.outputs.s3_bucket }}" \
            --delete \
            --cache-control "public,max-age=31536000,immutable" \
            --exclude "index.html"
          aws s3 cp apps/backoffice/dist/index.html \
            "s3://${{ steps.tf-post.outputs.s3_bucket }}/index.html" \
            --cache-control "no-cache,no-store,must-revalidate"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "${{ steps.tf-post.outputs.cf_id }}" \
            --paths "/*"

      # ── Summary ───────────────────────────────────────────────────────────────
      - name: Print deployment URLs
        run: |
          echo "API:        ${{ steps.tf-post.outputs.api_url }}"
          echo "Backoffice: ${{ steps.tf-post.outputs.cf_domain }}"
